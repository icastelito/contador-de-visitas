# Servidor
PORT=3000
NODE_ENV=development

# PostgreSQL
DB_HOST=postgres
DB_PORT=5432
DB_NAME=contador_visitas
DB_USER=postgres
DB_PASSWORD=sua_senha_segura

# Aplicação
COOKIE_SECRET=sua_chave_secreta_aqui

# Admin (para registrar novos sites via /api/register)
ADMIN_USER=seu_usuario_admin
ADMIN_PASSWORD=sua_senha_forte_aqui

# Base URL (usado para gerar scripts automaticamente)
BASE_URL=http://localhost:3000

# Customização padrão dos badges
DEFAULT_BADGE_STYLE=flat
DEFAULT_BADGE_COLOR=4c1
DEFAULT_LABEL=Visitas
